os:
  - linux
  - osx

language: cpp

dist: bionic

compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - g++-7
      - gcc-7
      - cmake

script:
  - mkdir build
  - cd build
  - cmake .. && make
  - ctest --verbose CTestTestfile.cmake
