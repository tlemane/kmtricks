os:
  - linux

language: cpp

dist: bionic

compiler:
  - gcc

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - g++-7
      - gcc-7
      - cmake

script:
  - export CC=gcc-7
  - export CXX=g++-7
  - mkdir build
  - cd build
  - cmake .. && make
  - make test
